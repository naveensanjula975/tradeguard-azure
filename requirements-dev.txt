# Development dependencies for TradeGuard (local dev + CI tooling)
# These are NOT production dependencies.

# ── Testing ────────────────────────────────────────────────────────
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-cov==5.0.0
pytest-mock==3.14.0
httpx==0.27.0           # for TestClient + async HTTP in tests
testcontainers==4.5.1   # spin up real Postgres in integration tests

# ── Code Quality ───────────────────────────────────────────────────
ruff==0.4.7             # formatter + linter (replaces black + flake8 + isort)
mypy==1.10.0            # static type checker
types-requests==2.32.0.20240602
types-pyyaml==6.0.12.20240311

# ── Security ───────────────────────────────────────────────────────
bandit==1.7.9           # Python SAST
pip-audit==2.7.3        # CVE scanning for dependencies

# ── Pre-commit ─────────────────────────────────────────────────────
pre-commit==3.7.1

# ── Commit tooling ─────────────────────────────────────────────────
commitizen==3.27.0      # conventional commits + changelog generation

# ── Load testing (optional, CI gate on release branches) ──────────
locust==2.29.0

# ── Debugging ─────────────────────────────────────────────────────
ipython==8.25.0
rich==13.7.1            # pretty terminal output for scripts
