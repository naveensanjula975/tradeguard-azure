export default function RulesPage() {
  return (
    <div>
      <h1 style={{ marginBottom: '1.5rem', fontSize: '1.75rem' }}>Risk Rule Management</h1>
      <div className="card">
        <p style={{ color: 'var(--text-secondary)' }}>Configure rule thresholds and enable/disable risk detection rules.</p>
      </div>
    </div>
  );
}
